#include "core/craft.h"

namespace Craft {
    Window *window = nullptr;
    RenderManager *renderManager = nullptr;
    World *world = nullptr;
    Player *player = nullptr;
    Debug *debug = nullptr;
}